%div.cell.feed
  %h4 Poptart Playlist
  %p
    A test in aggregation.
  %p
    This page is a clone of my feed, sing up to start cusotmizing yours!
  
  %ul
    - @listu.each do |userentry|
      %li 
        =userentry.email
        %br
        =userentry.id
        
        
-@userfeed.feeds.each do |feedin|  
  %div.cell.feed
    %h5
      =feedin.feed_title
      -if feedin.feed_type == "pinterest"
        's Pinterest
      %span
        (
        =feedin.feed_items.count
        )
    
    =haml :'partials/feed', :locals => {:feedin => feedin}